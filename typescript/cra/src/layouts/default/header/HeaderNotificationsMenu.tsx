import { Box, useTheme } from '@mui/material';

const HeaderNotificationsMenu = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        alignItems: 'center'
      }}
    >
      Notifications
    </Box>
  );
};

export { HeaderNotificationsMenu };
