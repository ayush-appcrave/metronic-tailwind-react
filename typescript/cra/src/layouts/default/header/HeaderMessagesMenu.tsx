import { Box, useTheme } from '@mui/material';

const HeaderMessagesMenu = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        alignItems: 'center'
      }}
    >
      Messages
    </Box>
  );
};

export { HeaderMessagesMenu };
