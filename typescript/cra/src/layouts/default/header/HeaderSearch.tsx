import { Box, useTheme } from '@mui/material';

const HeaderSearch = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        alignItems: 'center'
      }}
    >
      Search
    </Box>
  );
};

export { HeaderSearch };
