import { ILayoutConfig } from "../";

const defaultLayoutConfig: ILayoutConfig = {
  name: "default-layout",
  options: {
    sidebar: {
      collapse: false
    }
  }
};

export { defaultLayoutConfig };
